version: '3.8'

services:
  web:
    build: .
    container_name: node_app
    image: prashansa_nodejss
    ports:
      - "8000:8000"
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
